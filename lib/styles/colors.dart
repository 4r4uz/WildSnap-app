import 'package:flutter/material.dart';

class AppColors {
  // Colores base oscuros
  static const Color darkBackground = Color(0xFF0F172A);    // Fondo principal
  static const Color cardDark = Color(0xFF1E293B);          // Tarjetas
  static const Color surfaceDark = Color(0xFF334155);       // Superficies
  static const Color accentDark = Color(0xFF475569);        // Acentos suaves
  
  // Colores primarios - Verde Naturaleza
  static const Color primary = Color(0xFF10B981);           // Verde principal
  static const Color primaryLight = Color(0xFF34D399);      // Verde claro
  static const Color primaryDark = Color(0xFF059669);       // Verde oscuro
  
  // Colores secundarios - Azul Profundo
  static const Color secondary = Color(0xFF3B82F6);         // Azul principal
  static const Color secondaryLight = Color(0xFF60A5FA);    // Azul claro
  static const Color secondaryDark = Color(0xFF2563EB);     // Azul oscuro
  
  // Colores acento - Tierra y Vida
  static const Color accentAmber = Color(0xFFF59E0B);       // Ámbar/ocre
  static const Color accentEmerald = Color(0xFF10B981);     // Esmeralda
  static const Color accentViolet = Color(0xFF8B5CF6);      // Violeta
  
  // Colores semánticos
  static const Color success = Color(0xFF10B981);           // Verde éxito
  static const Color warning = Color(0xFFF59E0B);           // Ámbar advertencia
  static const Color error = Color(0xFFEF4444);             // Rojo error
  static const Color info = Color(0xFF3B82F6);              // Azul información
  
  // Textos
  static const Color textPrimary = Color(0xFFF8FAFC);       // Texto principal
  static const Color textSecondary = Color(0xFFCBD5E1);     // Texto secundario
  static const Color textHint = Color(0xFF64748B);          // Texto hints
  
  // Bordes y divisores
  static const Color border = Color(0xFF334155);            // Bordes
  static const Color divider = Color(0xFF334155);           // Divisores
}
